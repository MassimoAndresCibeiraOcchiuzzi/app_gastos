# Copiar a .env.local y completar.

# Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# console.anthropic.com > API Keys. Sólo se usa en el servidor: nunca lleva
# el prefijo NEXT_PUBLIC_, así que no se filtra al navegador.
ANTHROPIC_API_KEY=
